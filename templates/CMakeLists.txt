# テンプレートファイルをインストールする
install(
    DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}"
    DESTINATION "${PROJECT_INSTALL_DATA_DIR}"
    PATTERN ".jinja" PERMISSIONS OWNER_READ GROUP_READ
    PATTERN "CMakeLists.txt" EXCLUDE
)